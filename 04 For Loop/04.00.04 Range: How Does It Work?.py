# The function range(min_value, max_value) generates the sequence of numbers:
# min_value, min_value + 1, ..., max_value - 1
# We'd like to stress: the number max_value is not included.

for i in range(4,8):
    print(i)
